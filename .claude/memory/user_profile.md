---
name: Jonathan Peris - Developer Profile
description: Senior developer maintaining multiple backend challenge implementations across .NET, Rust, Go, Python
type: user
---

- Maintains multiple implementations of the Rinha de Backend challenge (rinha2-back-end-dotnet, plus Rust, Go, Python variants)
- Shared infrastructure pattern: same PostgreSQL schema, stored procedures, NGINX config, and k6 tests across all implementations
- Values clean git history (linear/rebase-only), proper CI/CD, and GitHub best practices
- Uses centralized `.github` repo for community health files across all repos
- Prefers `gh` CLI over HTTP API for all GitHub operations
- Uses RTK (Rust Token Killer) for token-optimized CLI operations
